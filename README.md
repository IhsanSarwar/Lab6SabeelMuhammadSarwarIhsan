# Lab6SabeelMuhammadSarwarIhsan

Test your application. You should be able to add books and delete books. 
<strong>Works fine</strong>

Additional Steps:
1. Include a README that describe the webapp and how to deploy it

We are using a backend and frontend set up through a database software called Mongo - first we have to establish the backends connection with routes and servers then make sure we have our MongoDb is connected properly and storing our data and also being updated/deleted if the user does so. Frontend has been established with code from slides, by running npm run dev (backend) and npm start (fronted), we can enter both Port 5000 and Port 3000 to see our respective sides, when we add to the frontend it gets stored into the backend port and also we can see it in the DB. 


2. Define  2 additional features to this application and add that description in the README, then update the code to include those enhancements.

We just added more than the original alotted characteristics for the books given from the slides, adding published Date, Publisher, and pages. We also added styling to the front end, with a book card and form alongside polished buttons.